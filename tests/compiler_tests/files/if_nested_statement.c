#include <stdio.h>

int main() {
    int a = 10;
    if (a > 5) {
        printf("%d", a);
        if (a > 15) {
            printf("%d", a);
        } else {
            printf("%d", a);
        }
    }
}

