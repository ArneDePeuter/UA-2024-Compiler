#include <stdio.h>

int main() {
    int x = 3;
    switch (x) {
        case 1:
            printf("%d", x);
            break;
        case 2:
            printf("%d", x);
            break;
        default:
            printf("%d", x);
    }
}
