#include <stdio.h>

int main() {
    int number = 4;
    if (number > 5) {
        //printf("Greater than 5\n");
        printf("%d", number);
    } else {
        //printf("Less than or equal to 5\n");
        printf("%d", number);
    }
    return 0;
}
