#include <stdio.h>

int main() {
    int a = 5;
    int b = 3;
    int sum = a + b;
    int diff = a - b;
    int product = a * b;
    int quotient = a / b;

    if (sum > 10) {
        printf("%d", sum);
    } else {
        printf("%d", sum);
    }

    for (int i = 0; i < sum; i++) {
        printf("%d", i);
    }

    return 0;
}
